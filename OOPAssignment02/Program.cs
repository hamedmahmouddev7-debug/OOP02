namespace OOPAssignment02
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Question01
            // (A) A class is a reference type, while a struct is a value type.

            // (B) Classes support inheritance and code reuse, so they are easier to manage.
            #endregion

            #region Question02
            // (A) Shipment is the parent class.

            // (B) ExpressShipment is the child class because it inherits from Shipment 

            // (C) ExpressShipment inherits the TrackingCode property from Shipment.

            // (D) Inheritance allows code reuse and reduces code duplication, making programs easier to maintain and extend.
            #endregion
        }
    }
}
   